meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm install --include=dev && npm run build && cd backend && npm install --include=dev && npm run build && npx prisma generate

run:
  scriptName: backend/start.js
  persistenceMount: /data
  containerPort: 3000
